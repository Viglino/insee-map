{
  "name": "insee-map",
  "version": "1.0.0",
  "description": "ol-ext + parcel bundler",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel index.html --out-dir ./.dist",
    "build": "parcel build --public-url . --out-dir ./build index.html --no-source-maps"
  },
  "keywords": [
    "ol",
    "ol-ext",
    "insee",
    "map",
    "statistic"
  ],
  "author": "Viglino",
  "license": "MIT",
  "devDependencies": {
    "parcel-bundler": "^1.9.4"
  },
  "dependencies": {
    "ol-ext": "^3.1.2",
    "proj4": "^2.5.0"
  }
}
